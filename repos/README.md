# External Repository Registry

External repositories are tracked by metadata only. Do not vendor or copy whole repositories into this repo.

Use `repos/registry.yaml` for source links, why they matter, review cadence, and local application guidance.

Generated GitHub radar candidates live under `maintainer/radar/` because they are optional maintainer review artifacts, not part of the ordinary user path.
